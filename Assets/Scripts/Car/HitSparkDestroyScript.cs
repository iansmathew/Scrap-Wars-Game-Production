using UnityEngine;

public class HitSparkDestroyScript : MonoBehaviour {

	// Use this for initialization
	void Start () {
        Destroy(gameObject, 0.3f);
	}
	
	// Update is called once per frame
	void Update () {
		
	}
}
