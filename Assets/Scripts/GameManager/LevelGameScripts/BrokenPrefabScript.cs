using UnityEngine;

public class BrokenPrefabScript : MonoBehaviour {

	// Use this for initialization
	void Start () {
        Destroy(gameObject, 3.0f);
	}
	
	// Update is called once per frame
	void Update () {
		
	}
}
